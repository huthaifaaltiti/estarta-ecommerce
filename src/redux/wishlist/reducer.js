// Wishlist reducer
const initialState = {
  wishlistItems: [],
};

function WishlistReducer(state = initialState, action) {
  switch (action.type) {
    case value:
      break;

    default:
      return state;
  }
}

export default WishlistReducer;
